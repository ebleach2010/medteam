{
  "name": "medteam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MedTeam — a silly top-down 3D physics ED triage game (nurse + doctor co-op).",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "playwright test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.114.0",
    "@dimforge/rapier3d-compat": "^0.14.0",
    "three": "^0.166.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.0"
  }
}
